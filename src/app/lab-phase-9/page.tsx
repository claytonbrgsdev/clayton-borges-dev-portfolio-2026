import { ChromaLab } from "@/components/ChromaLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Chrome Field — Phase 9" };

export default function LabPhase9Page() {
  return <ChromaLab />;
}
