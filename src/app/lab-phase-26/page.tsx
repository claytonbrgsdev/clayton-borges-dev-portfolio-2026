import { SuperpositionLab } from "@/components/SuperpositionLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "SuperpositionLab — Phase 26" };

export default function LabPhase26Page() {
  return <SuperpositionLab />;
}
