import { KuromatoLab } from "@/components/KuromatoLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Kuramoto — Phase 22" };

export default function LabPhase22Page() {
  return <KuromatoLab />;
}
