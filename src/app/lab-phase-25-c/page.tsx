import { SpiroLab } from "@/components/SpiroLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "SpiroLab — Phase 25c" };

export default function LabPhase25cPage() {
  return <SpiroLab />;
}
