import { DepthLab } from "@/components/DepthLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Depth Charge — Phase 11" };

export default function LabPhase11Page() {
  return <DepthLab />;
}
