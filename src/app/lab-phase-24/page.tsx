import { MandelbrotLab } from "@/components/MandelbrotLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Mandelbrot — Phase 24" };

export default function LabPhase24Page() {
  return <MandelbrotLab />;
}
