import { NexusLab } from "@/components/NexusLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Nexus — Phase 16" };

export default function LabPhase16Page() {
  return <NexusLab />;
}
