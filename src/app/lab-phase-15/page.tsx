import { OscilloLab } from "@/components/OscilloLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Phosphor — Phase 15" };

export default function LabPhase15Page() {
  return <OscilloLab />;
}
