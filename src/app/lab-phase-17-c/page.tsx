import { FrostLab } from "@/components/FrostLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Frost — Phase 17c" };

export default function LabPhase17cPage() {
  return <FrostLab />;
}
