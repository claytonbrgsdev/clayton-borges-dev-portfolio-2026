import { WaveOpticsLab } from "@/components/WaveOpticsLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Wave Optics — Phase 23b" };

export default function LabPhase23bPage() {
  return <WaveOpticsLab />;
}
