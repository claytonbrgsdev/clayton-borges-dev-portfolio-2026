import { RecurLab } from "@/components/RecurLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Recurrence — Phase 12" };

export default function LabPhase12Page() {
  return <RecurLab />;
}
