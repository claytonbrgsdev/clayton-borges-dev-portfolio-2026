import { MorphLab } from "@/components/MorphLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Morph Flux — Phase 10" };

export default function LabPhase10Page() {
  return <MorphLab />;
}
