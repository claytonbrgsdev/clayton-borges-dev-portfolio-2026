import { GasLab } from "@/components/GasLab";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Molecular Gas — Phase 25" };

export default function LabPhase25Page() {
  return <GasLab />;
}
